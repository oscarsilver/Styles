import UIKit

public struct BorderStyle {
    let color: UIColor
    let width: CGFloat

    public init(color: UIColor, width: CGFloat) {
        self.color = color
        self.width = width
    }
}
